---
title: Reference
description: Complete exports table for every @f0rbit/forge subpath, generated from a typed data module.
---

import { sections, totalExportCount } from "../../data/exports.ts";

The full public surface of `@f0rbit/forge` — every export across the five subpaths, with kind, signature, and a one-line description. Sourced from a typed `data/exports.ts` module that doubles as the data behind [`/llms.txt`](/forge/llms.txt).

For LLM consumption, fetch the plain-text version at `/forge/llms.txt`. For deep-dive prose, follow the per-topic pages in the sidebar.

**Total exports:** {totalExportCount} across {sections.length} subpaths.

{sections.map((section) => (
  <section>
    <h2 id={section.subpath.replace(/[^a-z0-9]/gi, "-").toLowerCase()}>
      <code>{section.subpath}</code>
    </h2>
    <p>{section.description}</p>
    {section.categories.map((category) => (
      <div>
        <h3>{category.name}</h3>
        {category.description && <p>{category.description}</p>}
        <table>
          <thead>
            <tr>
              <th>Name</th>
              <th>Kind</th>
              <th>Signature</th>
              <th>Description</th>
            </tr>
          </thead>
          <tbody>
            {category.exports.map((entry) => (
              <tr>
                <td><code>{entry.name}</code></td>
                <td>{entry.kind}</td>
                <td>{entry.signature ? <code>{entry.signature}</code> : "—"}</td>
                <td>{entry.description}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    ))}
  </section>
))}
