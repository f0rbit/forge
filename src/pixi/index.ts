// TODO(P5): export boot, sprite, sprite_c, camera, assets
// TODO(P5): export anim_sync (anim.sync system)
// TODO(P5): export browserSource (DOM + Gamepad InputSource)
// TODO(P5): export debug renderer (Pixi Graphics-backed)
// TODO(P5): export palette overlay (Pixi Text-backed)
//
// Every fallible boundary returns Result<T, EngineError> from @f0rbit/corpus.
// `pixi.js` is a peer dependency; this is the only subpath allowed to import it.

export {};
